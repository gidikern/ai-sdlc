# {{PROJECT_NAME}}

## Project Overview

{{PROJECT_DESCRIPTION}}

## AI-SDLC Integration

This project lives under the ai-sdlc parent directory and inherits:
- 9 specialist agent skills via relative paths (`../../skills/`)
- Workflow commands and files (synced from parent)
- Development standards and best practices

### Inherited Skills

| Skill | Source | Status |
|-------|--------|--------|
| product-manager | ai-sdlc | ✅ Using base |
| architect | ai-sdlc | ✅ Using base |
| developer | ai-sdlc | ✅ Using base |
| reviewer | ai-sdlc | ✅ Using base |
| legal-consultant | ai-sdlc | ✅ Using base |
| facilitator | ai-sdlc | ✅ Using base |
| ux-researcher | ai-sdlc | ✅ Using base |
| growth-strategist | ai-sdlc | ✅ Using base |
| data-analyst | ai-sdlc | ✅ Using base |

### Local Overrides

To override a base skill, create `.claude/skills/<skill-name>/SKILL.md` in this project.

Currently overridden:
- (none)

## Project-Specific Context

### Domain

{{DOMAIN_CONTEXT}}

### Technical Constraints

{{TECHNICAL_CONSTRAINTS}}

### Key Decisions

See `docs/architecture/adr/` for Architecture Decision Records.

## Workflows

### Starting New Work

1. **New feature/product**: Start with Discovery
   ```
   "Let's explore [feature/idea]"
   → Product Manager (Discovery Mode)
   ```

2. **Ready to define**: Move to Definition
   ```
   "Let's define what we're building"
   → Product Manager (Definition Mode)
   ```

3. **Ready to design**: Move to Architecture
   ```
   "Design the architecture"
   → Architect
   ```

### Project Structure

```
{{PROJECT_NAME}}/
├── .claude/
│   ├── settings.json     ← Skill configuration (references ../../skills/)
│   ├── commands/         ← Workflow commands (synced from parent)
│   ├── workflows/        ← Workflow files (synced from parent)
│   └── skills/           ← Local skill overrides
├── docs/
│   ├── product/          ← PRD, discovery, user stories
│   ├── architecture/     ← System design, ADRs
│   └── legal/            ← Risk assessments, compliance
├── src/                  ← Source code
└── tests/                ← Test files
```

## Commands

### Workflow Commands (Available in this project)

| Command | Purpose |
|---------|---------|
| `/cross-functional-discovery` | Multi-agent discovery process |

### Framework Commands (Run from parent ai-sdlc directory)

| Command | Purpose |
|---------|---------|
| `/release <version>` | Sync new skills/workflows to this project |

Add project-specific commands in `.claude/commands/` as needed.
